# AGENTS.md

本文件用于约束后续 AI Agent 在本项目中的开发、提交和协作行为。所有 Agent 在修改本仓库前都应先阅读并遵守本文件。

## 项目范围

- 项目根目录：`E:\Code\voice_translation_agent`
- 远程仓库：`https://github.com/llamapw/voice_translation_agent.git`
- 文档目录：`文档/`
- `文档/` 已加入 `.gitignore`，默认不提交到 Git。

## 基本协作原则

- 每次修改前先查看当前仓库状态，避免覆盖用户或其他 Agent 的未提交改动。
- 不要主动执行破坏性 Git 操作，例如 `git reset --hard`、强制覆盖、强制推送，除非用户明确要求。
- 修改应保持小而清晰，优先完成一个独立目标。
- 大功能必须拆分为多个可独立验证的小任务和小 PR。
- 主分支在任意时间都应保持可运行、可演示、可复现。

## PR 规范

所有 PR 必须基于“新增或修改单一功能”的原则提交。

### PR 粒度

- 每个 PR 只做一件事。
- 每个 PR 只实现或修改一个单一功能。
- 鼓励尽可能小、粒度尽可能细的 PR。
- 大功能应拆分为多个独立 PR，按阶段分步提交。

### PR 标题

PR 标题必须用一句话说明本 PR 新增或修改了什么。

示例：

```text
添加语音翻译任务的基础配置加载逻辑
```

### PR 描述

PR 描述必须包含以下内容：

```markdown
## 功能描述

说明该功能的作用与使用方式。

## 实现思路

简要说明技术选型、核心实现逻辑或关键改动点。

## 测试方式

说明如何验证该功能正常运行。
```

### PR 合并要求

- 合并后主分支代码必须保持可运行状态。
- 合并后应能在任意时间复现演示效果。
- 未说明测试方式或无法验证的 PR 不应合并。

## Commit 规范

Commit 应与 PR 一样保持清晰、单一、可验证。

### Commit 粒度

- 每个 commit 只做一件事。
- 不要把无关修改混入同一个 commit。
- 大改动应拆成多个逻辑清晰的小 commit。
- 不要提交临时文件、调试产物、缓存文件或被 `.gitignore` 忽略的文档目录内容。

### Commit Message

Commit message 应清楚说明本次提交做了什么。

建议格式：

```text
<类型>: <一句话说明改动>
```

常用类型：

- `feat`: 新增功能
- `fix`: 修复问题
- `docs`: 文档修改
- `refactor`: 重构，不改变外部行为
- `test`: 新增或修改测试
- `chore`: 构建、配置、依赖等杂项维护

示例：

```text
feat: 添加语音翻译任务配置读取
fix: 修复空音频输入时的异常处理
docs: 添加项目 Git 协作规范
```

## 开发流程

1. 开始前查看仓库状态：

   ```powershell
   git status --short
   ```

2. 明确本次任务只对应一个独立目标。
3. 修改代码或文档。
4. 运行必要的验证命令。
5. 再次查看 Git 状态，确认只包含本次任务相关改动。
6. 如需提交，按 Commit 规范生成提交信息。
7. 如需发起 PR，按 PR 规范补齐标题、功能描述、实现思路和测试方式。

## 验证要求

- 每次修改后应尽量运行与改动相关的测试、构建或静态检查。
- 如果项目暂时没有测试命令，应至少进行可复现的手动验证，并在回复或 PR 描述中说明。
- 不要声称“已完成”“已通过”或“可运行”，除非已经执行过对应验证。
- 如果无法验证，应明确说明无法验证的原因和剩余风险。

## Agent 回复要求

- 默认使用中文回复。
- 回复应简洁说明完成了什么、修改了哪些文件、如何验证。
- 涉及文件路径时使用完整路径，例如 `E:\Code\voice_translation_agent\AGENTS.md`。
- 如果需要用户确认远程推送、创建 PR 或执行高风险操作，应先询问用户。
