# FastAPI backend framework and ASGI server.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Required by FastAPI when receiving multipart/form-data uploads.
python-multipart>=0.0.9

# Configuration loading for .env and typed runtime settings.
pydantic-settings>=2.6.0

# Test runner for backend unit and API tests.
pytest>=8.3.0
httpx>=0.27.0
